RepositoryJPA
-------------

This project intends to be a generic repository for JPA instances

Validar as entidades
Inclusao de objetos
Alteracao
Exclusao logica
Carregar
Example
Listagem sem parametros
Consulta parametrizada

@Entity
public class Entidade implements Serializable {
	
	@Id
	private Long id;
	
	@Version
	private Integer version;
	
	private Boolean active;
	
	public Entidade(){
	
	}
	
}

Delivery d = new Delivery();
d.select(X.class);
d.whose(new Equals("atributo", valor));
d.fetch();
d.fetchAll();